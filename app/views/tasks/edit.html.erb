<%= form_with model: [@genre, @task], local: true do |f| %>

  <%= f.label :name%>
  <%= f.text_area :name, placeholder:"タスク名"%>

  <%= f.label :text%>
  <%= f.text_area :text, placeholder:"メモ"%>

  <%= f.submit "更新する"%>
<% end %>