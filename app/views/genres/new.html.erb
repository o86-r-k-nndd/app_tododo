<%= form_with model: @genre, local: true do |f| %>

  <%= f.label :name%>
  <%= f.text_area :name, placeholder:"ジャンル名"%>

  <%= f.label :text%>
  <%= f.text_area :text, placeholder:"メモ"%>

  <%= f.submit "投稿する"%>
<% end %>